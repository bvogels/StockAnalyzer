import stockanalyzer.ui.UserInterface;

public class MCP {

	public static void main(String args[]) {
		UserInterface ui = new UserInterface(); // Creates a new UserInterface object with name ui
		ui.start(); // Calls the start method of UserInterface ui.
	}
}
